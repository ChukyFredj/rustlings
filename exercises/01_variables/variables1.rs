// variables1.rs
//
// Make me compile!
//
// Execute `rustlings hint variables1` or use the `hint` watch subcommand for a
// hint.

// x n'est pas déclaré, il faut le déclarer avant de l'utiliser avec let

fn main() {
    let x = 5;
    println!("x has the value {}", x);
}
